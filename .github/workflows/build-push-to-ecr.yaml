name: Build and Push to ECR (Reusable)

on:
  workflow_call:
    inputs:
      service_name:
        required: true
        type: string

      aws_region:
        required: false
        type: string
        default: eu-central-1

      ecr_namespace:
        required: false
        type: string
        default: sabahhub

      dockerfile:
        required: false
        type: string
        default: ./Dockerfile

      context:
        required: false
        type: string
        default: .

      next_public_api_url:
        required: false
        type: string
        default: "https://api.planandeat.az/api"

      role_to_assume:
        required: true
        type: string

    outputs:
      service_name:
        description: "Service name passed by caller"
        value: ${{ jobs.build-and-push.outputs.service_name }}
      environment:
        description: "Derived environment (prod for main, dev otherwise)"
        value: ${{ jobs.build-and-push.outputs.environment }}
      image_tag:
        description: "Image tag (commit SHA)"
        value: ${{ jobs.build-and-push.outputs.image_tag }}
      ecr_repository:
        description: "ECR repository path (namespace/env/service)"
        value: ${{ jobs.build-and-push.outputs.ecr_repository }}
      registry:
        description: "ECR registry (account.dkr.ecr.region.amazonaws.com)"
        value: ${{ jobs.build-and-push.outputs.registry }}
      image_ref_sha:
        description: "Full image reference with SHA tag"
        value: ${{ jobs.build-and-push.outputs.image_ref_sha }}
      image_ref_latest:
        description: "Full image reference with latest tag"
        value: ${{ jobs.build-and-push.outputs.image_ref_latest }}

permissions:
  id-token: write
  contents: read

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    environment: ${{ github.ref_name == 'main' && 'prod' || 'dev' }}

    env:
      AWS_REGION: ${{ inputs.aws_region }}
      ENVIRONMENT: ${{ github.ref_name == 'main' && 'prod' || 'dev' }}
      ECR_REPOSITORY: ${{ inputs.ecr_namespace }}/${{ github.ref_name == 'main' && 'prod' || 'dev' }}/${{ inputs.service_name }}
      IMAGE_TAG: ${{ github.sha }}

    outputs:
      service_name: ${{ steps.meta.outputs.service_name }}
      environment: ${{ steps.meta.outputs.environment }}
      image_tag: ${{ steps.meta.outputs.image_tag }}
      ecr_repository: ${{ steps.meta.outputs.ecr_repository }}
      registry: ${{ steps.meta.outputs.registry }}
      image_ref_sha: ${{ steps.meta.outputs.image_ref_sha }}
      image_ref_latest: ${{ steps.meta.outputs.image_ref_latest }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials via GitHub OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ inputs.role_to_assume }}
          aws-region: ${{ inputs.aws_region }}

      - name: Ensure ECR repository exists
        run: |
          set -euo pipefail

          echo "Environment: ${ENVIRONMENT}"
          echo "ECR repo: ${ECR_REPOSITORY}"

          if ! aws ecr describe-repositories --repository-names "${ECR_REPOSITORY}" --region "${AWS_REGION}" > /dev/null 2>&1; then
            echo "Creating ${ECR_REPOSITORY}"
            aws ecr create-repository \
              --repository-name "${ECR_REPOSITORY}" \
              --region "${AWS_REGION}" \
              --image-scanning-configuration scanOnPush=true \
              --encryption-configuration encryptionType=AES256
          else
            echo "Repository already exists"
          fi

      - name: Login to Amazon ECR
        id: ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          push: true
          context: ${{ inputs.context }}
          dockerfile: ${{ inputs.dockerfile }}
          tags: |
            ${{ steps.ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ env.IMAGE_TAG }}
            ${{ steps.ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Export outputs
        id: meta
        run: |
          set -euo pipefail

          REGISTRY="${{ steps.ecr.outputs.registry }}"
          ECR_REPO="${ECR_REPOSITORY}"
          TAG="${IMAGE_TAG}"
          ENVN="${ENVIRONMENT}"
          SVC="${{ inputs.service_name }}"

          echo "service_name=${SVC}" >> "$GITHUB_OUTPUT"
          echo "environment=${ENVN}" >> "$GITHUB_OUTPUT"
          echo "image_tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "ecr_repository=${ECR_REPO}" >> "$GITHUB_OUTPUT"
          echo "registry=${REGISTRY}" >> "$GITHUB_OUTPUT"
          echo "image_ref_sha=${REGISTRY}/${ECR_REPO}:${TAG}" >> "$GITHUB_OUTPUT"
          echo "image_ref_latest=${REGISTRY}/${ECR_REPO}:latest" >> "$GITHUB_OUTPUT"
